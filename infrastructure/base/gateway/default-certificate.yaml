apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-default
  namespace: gateway-default
spec:
  dnsNames:
    - "${GATEWAY_DOMAIN}"
    - "*.${GATEWAY_DOMAIN}"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: wildcard-tls
