apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  # must match the live Helm release name, otherwise this installs a
  # second parallel stack instead of adopting the running one
  releaseName: kube-prometheus-stack
  targetNamespace: monitoring
  interval: 1h
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: kube-prometheus-stack
      version: 91.2.3
      sourceRef:
        kind: HelmRepository
        name: prometheus
        namespace: flux-system
  values:
    prometheusOperator:
      tls:
        enabled: false
      admissionWebhooks:
        enabled: false
    alertmanager:
      enabled: true
      route:
        main:
          enabled: true
          hostnames:
            - alerts.${GATEWAY_DOMAIN}
          parentRefs:
            - name: cilium
              namespace: gateway-default
              sectionName: https
      config:
        global:
          smtp_smarthost: smtp.mailer.svc.cluster.local:25
          smtp_from: ${ALERT_EMAIL_FROM}
          smtp_require_tls: false
        route:
          group_by:
            - alertname
            - namespace
            - service
          group_wait: 30s
          repeat_interval: 30m
          receiver: owner
          # defining routes replaces the chart's list, so Watchdog is repeated
          # here. InfoInhibitor exists only to drive inhibition and is never
          # meant to notify; its built-in muting rule is not taking effect on
          # alertmanager 0.34, so route it to the null receiver explicitly.
          routes:
            - receiver: "null"
              matchers:
                - alertname = "Watchdog"
            - receiver: "null"
              matchers:
                - alertname = "InfoInhibitor"
        receivers:
          - name: "null"
          - name: owner
            email_configs:
              - to: ${ALERT_EMAIL_TO}
                require_tls: false
                send_resolved: true
      alertmanagerSpec:
        externalUrl: https://alerts.${GATEWAY_DOMAIN}
        storage:
          volumeClaimTemplate:
            metadata:
              name: alertmanager-pvc
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 1Gi
    defaultRules:
      create: true
      rules:
        kubeProxy: false
    prometheus:
      enabled: true
      route:
        main:
          enabled: true
          hostnames:
            - prometheus.${GATEWAY_DOMAIN}
          parentRefs:
            - name: cilium
              namespace: gateway-default
              sectionName: https
      prometheusSpec:
        externalUrl: https://prometheus.${GATEWAY_DOMAIN}
        retentionSize: 40GiB
        storageSpec:
          volumeClaimTemplate:
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 50Gi
    grafana:
      enabled: true
      # the volume below is ReadWriteOnce, so a RollingUpdate deadlocks: the
      # replacement pod is scheduled to another node and waits forever for a
      # detach the old pod will not perform until the new one is ready
      deploymentStrategy:
        type: Recreate
      persistence:
        enabled: true
        existingClaim: grafana
    kubeEtcd:
      enabled: true
      endpoints:
        - ${ETCD_IP_1}
        - ${ETCD_IP_2}
        - ${ETCD_IP_3}
    kube-state-metrics:
      enabled: true
    prometheus-node-exporter:
      enabled: true
