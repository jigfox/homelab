apiVersion: v1
kind: Service
metadata:
  name: smtp
  namespace: mailer
  labels:
    app: smtp-relay
spec:
  type: ClusterIP
  selector:
    app: smtp-relay
  ports:
    - name: smtp
      protocol: TCP
      port: 25
      targetPort: 25
