apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-longhorn
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-sources
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/base/longhorn # Path to your environment-specific code
  prune: false # Deletes resources in K8s if they are deleted from Git
  wait: true # Flux will wait for all resources to be healthy before finishing

